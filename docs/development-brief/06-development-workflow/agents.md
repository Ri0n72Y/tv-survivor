# Agent 规则：开发工作流

## 开始前

确认是否采用隔离流程，并锁定需求、验收、明确不做和基线 SHA。

## 开发 Agent

只实现任务契约；不得擅自修改 PRD、主 TODO 或无关玩法。

## 审查 Agent

从需求节点独立分叉，基于代码和验收审查，不读取开发推理作为正确性依据。

## 测试 Agent

在独立环境验证指定 head；失败必须给出复现步骤和日志，不直接替开发结果背书。
