# Agent 规则：内容生产

## 默认做法

优先新增 Resource 数据；只有新行为原语才新增脚本。

## 禁止事项

- 不得手工维护中央内容数组。
- 不得在 `BuildState` 增加具体被动 ID 公式。
- 不得在 `WeaponManager` 增加具体武器 ID 分支。
- 不得绕过 RewardService 直接写奖励结果。

## 每个内容提交必须包含

ID、展示文案、等级/数值、依赖、运行时实现、奖励可用性、UI 表现、自动校验和手动验证步骤。
