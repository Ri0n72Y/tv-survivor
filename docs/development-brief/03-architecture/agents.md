# Agent 规则：工程架构

## 绝对边界

- Catalog 不得拥有运行时实例。
- `BuildState` 不得依赖场景节点。
- UI 不得直接写权威构筑状态。
- 新普通内容不得要求修改中央静态数组。
- 多效果结算不得依赖 Dictionary、Node 或 Signal 的隐式顺序。

## 架构变更交付

必须提供 Mermaid 图，覆盖组件、依赖、输入输出、状态流转、生命周期、失败处理以及并发/递归限制。

## 测试同步

每次架构改动必须更新：待验证 head SHA、行为契约、自动测试、手动回归、预期结果和执行状态。
